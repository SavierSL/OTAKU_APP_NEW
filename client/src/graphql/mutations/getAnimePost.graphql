query getAnimePostComment($animePostId: Int!) {
  getAnimePostComment(animePostId: $animePostId) {
    hasMore
    allComments {
      id
      comment
      commentor {
        id
        username
      }
    }
  }
}
